**Calculator**: Simple safe arithmetic evaluator

- **Usage**: Run the REPL with `python calculator.py` or import `evaluate`:

```
from calculator import evaluate
print(evaluate("2 + 3*4"))
```

- **Run tests**: install `pytest` then run:

```
pytest -q
```

This module evaluates simple arithmetic expressions safely using Python's AST.
# X-PROJECTS
